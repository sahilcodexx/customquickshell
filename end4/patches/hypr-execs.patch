--- hypr/custom/execs.lua
+++ hypr/custom/execs.lua
@@ -1,5 +1,11 @@
 -- Custom user execs
 hl.on("hyprland.start", function ()
-    -- Put your custom exec-once commands here
+    -- Screentime card (standalone quickshell config; opened via /screentime)
+    os.execute("pkill -f 'quickshell.*screentime-card' 2>/dev/null")
+    os.execute("qs -p /home/sahilcodex/.config/quickshell/screentime-card &")
+    -- GitHub card (standalone quickshell config; opened via /github)
+    os.execute("pkill -f 'quickshell.*github-card' 2>/dev/null")
+    os.execute("qs -p /home/sahilcodex/.config/quickshell/github-card &")
 end)
 
 -- Test keybind to see if SUPER+SHIFT+W triggers
