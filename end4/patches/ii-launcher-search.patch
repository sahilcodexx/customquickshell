--- ii/services/LauncherSearch.qml
+++ ii/services/LauncherSearch.qml
@@ -83,6 +83,18 @@
         {
             action: "screentime",
             execute: () => {
                 Quickshell.execDetached(["qs", "-p", Quickshell.env("HOME") + "/.config/quickshell/screentime-card", "ipc", "call", "screentime", "toggle"]);
             }
         },
+        {
+            action: "github",
+            execute: () => {
+                Quickshell.execDetached(["qs", "-p", Quickshell.env("HOME") + "/.config/quickshell/github-card", "ipc", "call", "github", "toggle"]);
+            }
+        },
         {
             action: "light",
             execute: () => {
